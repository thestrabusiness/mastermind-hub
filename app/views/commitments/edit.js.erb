document
  .getElementById('<%= dom_id(@commitment, 'body') %>')
  .insertAdjacentHTML('afterend', "<%= j render 'inline_form', commitment: @commitment, call: @call %>")

document
  .getElementById('<%= dom_id(@commitment, 'body') %>')
  .style
  .display = 'none'

document
  .getElementById('<%= dom_id(@commitment, 'pencil') %>')
  .classList.add('hidden')
