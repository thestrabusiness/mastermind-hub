.body
  You've been added to a mastermind group by
  = @user.full_name

  If you already have an account, sign in to check it out

  Don't have an account? Follow the link below:

  = link_to "Sign Up", sign_up_url(token: @invite.token)
