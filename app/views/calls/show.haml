.call-details-container
  .call-details-columns
    .title
      -if @page.viewing_todays_call?
        Today's Call:
      - else
        Call on:
      = DateFormatter.month_day_time(@page.call.scheduled_on)

    .column.column-left
      = render 'commitment_cards'

    .column.column-right
      = render 'timer_card'
      = render 'notes_card'
